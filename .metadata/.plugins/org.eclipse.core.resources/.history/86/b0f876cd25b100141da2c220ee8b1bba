import controleur.Controleur_Client;
import vue.Vue_Client;
import modele.Modele_Client;

public class ClientApp {

	public static void main(String[] args) {
		Modele_Client client = new Modele_Client();
		Vue_Client vueClient = new Vue_Client(client);
		//Controleur_Client controleurClient = new Controleur_Client(client, vueClient);
		
		
		
		
		
		/*JFrame frame = new JFrame("Client");
		frame.setLayout(new GridLayout(2,1));
		
		JPanel panelInfos = new JPanel();
		panelInfos.setLayout(new GridLayout(2,2));
		panelInfos.add(new JLabel("identifiant :"));
		panelInfos.add(new JTextArea(""));
		panelInfos.add(new JLabel("mot de passe :"));
		panelInfos.add(new JTextArea(""));
		
		JPanel panelButtons = new JPanel();
		panelButtons.setLayout(new GridLayout(1,2));
		panelButtons.add(new JButton("Se connecter"));
		panelButtons.add(new JButton("S'inscrire"));
		
		frame.add(panelInfos);
		frame.add(panelButtons);
		
		frame.pack();
		frame.setDefaultCloseOperation(frame.EXIT_ON_CLOSE);
		frame.setVisible(true);*/
	}

}
